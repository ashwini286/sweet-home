/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['240group.com','media-cdn.tripadvisor.com','static.wixstatic.com','cdn.prod.website-files.com'], // Add your domain here
    },
};

export default nextConfig;
